name: aetherlink_perf
description: "Aetherlink in-app performance monitor: per-thread (build/raster) frame timing, percentiles, jank events with context, rule-based bottleneck diagnosis and an AI-friendly JSON export. Pure Dart/Flutter, zero extra dependencies."
version: 0.0.1
publish_to: 'none'
homepage:

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
